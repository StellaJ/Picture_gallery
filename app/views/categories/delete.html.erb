<div class="hero-unit">
<h1>Delete category</h1>

<%= form_for(:categories, :url=>{:action=> 'sakujo_suru', :id => @categories.id}) do |f| %>

<p>Are you shure you want delete this category?</p>

<p><%= @categories.name %></p>

<div style="text-align: center;">
	<%= link_to "Back", {:action => 'index'}, :class => 'btn btn-primary'%>
	<%= submit_tag 'Delete', :class => 'btn btn-danger' %>
</div>


<% end %>
</div>
