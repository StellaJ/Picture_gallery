<div class="hero-unit">
<h1>Delete page</h1>

<%= form_for(:pages, :url=>{:action=> 'sakujo_suru', :id => @pages.id}) do |f| %>

<p>Are you shure you want delete this page?</p>

<p><%= @pages.name %></p>

<div style="text-align: center;">
	<%= link_to "Back", {:action => 'index'}, :class => 'btn btn-primary'%>
	<%= submit_tag 'Delete', :class => 'btn btn-danger' %>
</div>


<% end %>
</div>
