<div class="hero-unit">
<h1>Show page</h1>


<table class="table table-striped">

	<tr>
	    <th>Category:</th>
	    <td><%= @page.category.name %></td>
    </tr>
	<tr>
	    <th>Name</th>
	    <td><%= @page.name %></td>
    </tr>
    <tr>
	    <th>Position</th>
	    <td><%= @page.position %></td>
    </tr>
    <tr>
	    <th>It is visible?</th>
	    <td><%= @page.visibility ? 'Yes' : 'No' %></td>
    </tr>
     <tr>
	    <th>Created:</th>
	    <td><%= @page.created_at %></td>
    </tr>
     <tr>
	    <th>Update:</th>
	    <td><%= @page.updated_at %></td>
    </tr>
</table>
<div style="text-align: center;">
	<%= link_to "Back", {:action => 'index'}, :class => 'btn btn-primary' %>
</div>

</div>
